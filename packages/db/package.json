{
  "name": "@logistack/db",
  "version": "0.1.0",
  "private": true,
  "description": "Database schema, migrations, and Drizzle ORM setup for Logistack",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "generate": "drizzle-kit generate:pg",
    "migrate": "drizzle-kit push:pg",
    "studio": "drizzle-kit studio",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.29.3",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9",
    "drizzle-kit": "^0.20.10",
    "typescript": "^5.3.3"
  }
}
